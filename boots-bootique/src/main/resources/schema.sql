CREATE TABLE BOOTS (
                       ID INT PRIMARY KEY,
                       TYPE VARCHAR(255),
                       SIZE DECIMAL(5,2),
                       QUANTITY INT,
                       MATERIAL VARCHAR(255)
);